server:
  port: 8082

spring:
  application:
    name: User
  datasource:
    url: jdbc:postgresql://172.17.0.2:5432/user_ms
    username: postgres
    password: 123
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:4646/eureka


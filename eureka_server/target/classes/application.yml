spring:
  application:
    name: Eureka Server

server:
  port: 8090


eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/
